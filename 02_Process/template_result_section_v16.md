# 估价结果段落对接报告 V16

- 执行时间：2026-06-01 14:54:43
- 修复原则：`1.地价确定的方法` 使用 `final_price_determination`；`2.确定估价结果` 使用 `valuation_result_statement`。
- 表题和结果表保留原位置，后续仍由表格字段与格式规范化逻辑处理。

## 自动生成的评估报告模板_清理后.docx
- 修复项：错位 final 占位符移除 0 个；`1.地价确定的方法` 替换 2 处；`2.确定估价结果` 替换 2 处；`（一）估价结论` 替换 2 处。
- 复核：状态=通过；final占位符=2；结果正文占位符=4；旧硬编码短语=0。

## 自动生成的评估报告模板.docx
- 修复项：错位 final 占位符移除 1 个；`1.地价确定的方法` 替换 2 处；`2.确定估价结果` 替换 2 处；`（一）估价结论` 替换 2 处。
- 复核：状态=通过；final占位符=2；结果正文占位符=4；旧硬编码短语=0。
